---
import '../../styles/global.css';
import Layout from '../../components/Layout.astro';
import ParticleBackground from '../../components/ParticleBackground.astro';
import { getTranslations } from '../../content/translations';

const t = getTranslations('ko');
---

<Layout lang="ko" title={t.meta.title} currentPage="home" lightHeader={false}>
	<!-- Hero Section -->
	<section class="relative bg-section-alt h-[42em] overflow-hidden flex items-center">
		<ParticleBackground />
		<div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full">
			<div class="max-w-2xl">
				<h1 class="text-5xl md:text-6xl lg:text-7xl text-heading mb-8 leading-none animate-fade-in-up [word-break:keep-all]">
					<span class="text-gradient">{t.hero.title}</span>
				</h1>
				<p class="text-lg text-body mb-8 animate-fade-in-up animate-delay-200">
					{t.hero.subtitle}
				</p>
				<a href="/ko/contact" class="btn-primary inline-block px-8 py-4 rounded-full text-lg animate-fade-in-up animate-delay-400">
					{t.hero.cta}
				</a>
			</div>
		</div>
	</section>

	<!-- KPIs -->
	<section class="py-16 bg-section">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
			<div class="mb-12">
				<h2 class="text-2xl md:text-3xl text-heading mb-4 animate-fade-in-up">심층 프로테오믹스 이해의 미래</h2>
				<p class="text-muted max-w-2xl animate-fade-in-up animate-delay-100">Orbitrap Astral 질량분석과 AI 기반 분석으로 연구에 필요한 깊이, 속도, 재현성을 동시에 제공합니다.</p>
			</div>
			<div class="grid grid-cols-1 md:grid-cols-3 gap-8">
				{t.kpis.items.map((kpi, i) => (
					<div class={`text-center glass-card rounded-xl p-8 animate-fade-in-up animate-delay-${(i + 1) * 100}`}>
						<div class="text-4xl md:text-5xl text-[var(--brand-teal)] mb-2">{kpi.value}</div>
						<p class="text-muted">{kpi.label}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<!-- Services -->
	<section id="services" class="py-16 bg-section-alt">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
			<div class="mb-12">
				<h2 class="text-2xl md:text-3xl text-heading mb-4 animate-fade-in-up">오믹스 연구의 모든 샘플, 하나의 통합 플랫폼</h2>
				<p class="text-muted max-w-2xl animate-fade-in-up animate-delay-100">세포 배양부터 임상 체액까지, 모든 샘플 유형에 최적화된 워크플로우를 제공합니다. 샘플을 선택하고 몇 주 안에 논문 출판 가능한 결과를 받으세요.</p>
			</div>
			<div class="flex flex-wrap justify-center gap-8">
				{t.services.items.map((service, i) => (
					<a href={`/ko/services/${service.slug}`} class={`glass-card rounded-xl p-6 text-center hover-glow transition-all hover:scale-105 min-w-[140px] animate-fade-in-up animate-delay-${(i + 1) * 100}`}>
						<div class="text-4xl mb-3 opacity-80">{service.icon}</div>
						<h3 class="text-sm text-body">{service.title}</h3>
					</a>
				))}
			</div>
		</div>
	</section>

	<!-- Testimonials (hidden)
	<section class="py-12 bg-slate-800">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
			<h2 class="text-2xl text-center text-white mb-6">{t.testimonials.title}</h2>
			<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
				{t.testimonials.items.map((testimonial) => (
					<div class="glass-card p-6 rounded-xl">
						<p class="text-slate-300 mb-4 italic">
							"{testimonial.quote}"
						</p>
						<p class="text-white">{testimonial.author}</p>
						<p class="text-sm text-slate-500">{testimonial.institution}</p>
					</div>
				))}
			</div>
		</div>
	</section>
	-->

	<!-- How It Works -->
	<section id="how-it-works" class="py-16 bg-section">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
			<h2 class="text-2xl md:text-3xl text-heading mb-12 animate-fade-in-up">{t.howItWorks.title}</h2>
			<div class="grid grid-cols-1 md:grid-cols-3 gap-8">
				{t.howItWorks.steps.map((step, i) => (
					<div class={`text-center animate-fade-in-up animate-delay-${(i + 1) * 100}`}>
						<div class="glass-card rounded-full w-20 h-20 flex items-center justify-center mx-auto mb-4 glow">
							<span class="text-2xl text-[var(--brand-green)]">{step.number}</span>
						</div>
						<h3 class="text-lg text-heading mb-2">{step.title}</h3>
						<p class="text-muted">{step.description}</p>
					</div>
				))}
			</div>
		</div>
	</section>
</Layout>
